<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentthat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_rt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_rt_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.FilteredPropertiesActivity"></activity>
        <activity android:name=".view.RentDetailsForOwner" />
        <activity android:name=".view.RentDetails" />
        <activity android:name=".view.AddPropertieActivity" />
        <activity android:name=".view.PropertiesActivity" />
        <activity android:name=".view.ProfileActivity" />
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>